{% extends 'base.html.twig' %}

{% block title %}Modification d'un propriétaire
{% endblock %}

{% block body %}
	<div class="container">
		<div class="jumbotron">
			<h1>Modification</h1>
		</div>
		{{ include('owner/_form.html.twig', {'button_label': 'Mise à jour'}) }}

		<div class="row">
			<div class="col-lg-10">
				<a href="{{ path('owner_index') }}">Retourner à la liste des propriétaires</a>
			</div>
			<div class="col-lg-2">
				{{ include('owner/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
