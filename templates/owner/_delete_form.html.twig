<form method="post" action="{{ path('owner_delete', {'id': owner.id}) }}" onsubmit="return confirm('Etes-vous certain de vouloir le supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ owner.id) }}">
    <button class="btn btn-danger btn-lg">Supprimer</button>
</form>
